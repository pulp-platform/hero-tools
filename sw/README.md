# sw-host
## Contains
* `libhero`: The user-space library to bridge the kernel driver with applications
* `libllvm`: The LLVM librairies compiled for rv64 (needed to compile libomp)
* `libonp`: All the OpenMP part of LLVM compiled for rv64 (contains the custom hero runtime libraries).
